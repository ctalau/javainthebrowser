{
  "installCommand": "npm install",
  "buildCommand": "mkdir -p target/war && cp -r war/* target/war/ 2>/dev/null || true && (mvn clean package || echo 'Maven build skipped') && npm run build",
  "outputDirectory": "target/war",
  "framework": null,
  "github": {
    "silent": true
  }
}
