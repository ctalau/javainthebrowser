-- replace System.out from JVM Natives
-- class loader based on fs
-- Make a full JRE using also the Unsafe and ConsoleOutputStream
-- Assign onClick handler

FileSystem
 - make a tool to create an archive of symbols.
 - convert it to hex-encoded JSON.
 - load it.
 - load the source file contents from a String.
