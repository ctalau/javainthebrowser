{
  "name": "javainthebrowser",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "cd packages/ui && npm run dev",
    "build": "cd packages/ui && npm run build",
    "preview": "cd packages/ui && npm run preview",
    "build:maven": "mvn clean package"
  }
}
